#quantos jogos serão gerados e sorteará 6 números de 1 a 60 para cada jogo, cadastrando tudo em uma lista composta

from random import randint
from time import sleep
jogos = qtd = 0
números = []

print ('-'*30)
print ('          JOGO NA MEGA SENA             ')
print ('-'*30)

qtd = int(input('Quantos jogos serão gerados? '))

for n in range(6):
    randint (1,61)
    if n not in números:
        números.append(j)

números.sort()

print ('Eu vou te ajudar a escolher os números. Processando...')
for i, l in enumerate {qtd}:
    print (f'Jogo {i+1}: {l}')
    sleep (2)
print(f'Aqui estão: {números} \n BOA SORTE!')
